export default function() {
    let name = 'zach';

    while (true) {
        let name = 'obama';
        document.write(name);
        document.write('<br />')
        break;
    }

    document.write(name);
}